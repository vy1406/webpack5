caching the files so new generating file will have a new name, and 
you will listen to that name change.

the hash will be generated if the file has been changed

ToDo:

show [contenthash]

npm run build:minify

show the hash.

npm run build:minify

show the hash not changed!

modify index.js

npm run build:minify



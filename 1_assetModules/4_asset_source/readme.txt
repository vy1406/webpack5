asset/source

reads the content of the file into a js string and injects into the bundle as is.
of course it doesnt generate a new file.

show the content of add-image.js: we import the altText
show that we teached webpack to read that file.
npm run build.
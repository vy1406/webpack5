creating multiple bundles

created two components:
leftSide
rightSide

created two indexes:
leftIndex
rightIndex 
- to represent accordingly

webpack.prod.js:
    entry changed to represent 2 entry points.

output:
    changing filename [name], instead of bundle, it will create by name of entries.

plugins
    same



ToDO:
npm run build:prod
show dist.